[workspace]
members = [
    "apollo-federation-types",
    "apollo-composition",
    "router-bridge",
]
resolver = "2"

# TODO: comment out before release
#[patch.crates-io]
#apollo-federation = { git = 'https://github.com/apollographql/router.git', branch = 'next' }

# Comment this in for local test builds
[patch.crates-io]
apollo-federation = { path = "../router/apollo-federation" }
