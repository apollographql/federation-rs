[workspace]
members = ["apollo-federation-types", "apollo-composition"]
resolver = "2"

[workspace.dependencies]
apollo-compiler = "1.30.0"
# apollo-federation = "=2.8.0-preview.0"
apollo-federation = { git = "https://github.com/apollographql/router.git", branch = "bnjjj/auto_invalidation" }
