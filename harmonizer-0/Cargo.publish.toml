#### ⚠️ DO NOT EDIT THIS FILE ⚠️ ####
## it is autogenerated in build.harmonizer.rs ##
[package]
name = "harmonizer"
version = "0.33.8"
authors = [ "Apollo Graph, Inc. <packages@apollographql.com>" ]
edition = "2018"
description = "Apollo Federation utility to compose a supergraph from subgraphs"
homepage = "https://www.apollographql.com/apollo-federation/"
documentation = "https://apollographql.com/docs/federation/"
repository = "https://github.com/apollographql/federation/"
license = "MIT"
readme = "README.md"
include = [ "dist/**/*", "js/**/*", "src/**/*", "Cargo.toml" ]
publish = true
build = "../build_harmonizer.rs"

[dependencies]
apollo-federation-types = { version = "0.1", path = "../apollo-federation-types" }
deno_core = "0.114.0"
serde = { version = "1", features = [ "derive" ]}
serde_json = "1"

[dev-dependencies]
insta = "1.8.0"

[build-dependencies]
semver = "1"
serde_json = "1"
toml_edit = "0.13"
which = "4.2.2"
