[package]
name = "apollo-composition"
version = "0.1.0"
license = "Elastic-2.0"
edition = "2021"

[dependencies]
apollo-compiler = "1.0.0-beta.19"
#apollo-federation = { path = "../../router/apollo-federation" }
apollo-federation = { git = "https://github.com/apollographql/router", branch = "dylan/use-apollo-compiler-locations" }
apollo-federation-types = { path = "../apollo-federation-types" }
either = "1.12.0"